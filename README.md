# A Basic Form with Firebase
#### For devs, clone and create service account key with your firebase project and save it by creating config/serviceAccountKey.json
<br />then do,
<br />```npm install```
<br />```npm start```
